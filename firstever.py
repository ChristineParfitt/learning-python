#we're finding out the first and last name of the user
first = input('What is your first name?')
last = input('and surname?')
# we're trying to get this to have a number input

old = input('how old are you?')
#change the input from a string to an integer
age = int(old)
print('Welcome', first, last, 'who is', age, 'years old')

